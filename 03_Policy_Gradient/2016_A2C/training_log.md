# A2C 訓練日誌

## CartPole-v1（2026-05-17）

| 引數 | 數值 |
|------|------|
| n_episodes | 2,000 |
| lr | 7e-4（RMSprop） |
| gamma | 0.99 |
| n_steps | 5 |
| gae_lambda | 0.95 |
| c_v | 0.5 |
| c_e | 0.01 |
| 網路 | 共享 MLP 256×256 |

### 訓練過程

| 回合 | Eval 平均回報 |
|------|-------------|
| 100 | 9.2 ± 0.6 |
| 500 | 9.5 ± 0.7 |
| 700 | 19.6 ± 2.8 |
| 1000 | 12.7 ± 1.3 |
| 1600 | 48.7 ± 13.8 |
| 1700 | 169.6 ± 75.5 |
| 1800 | **357.5 ± 72.7**（峰值） |
| 1900 | 49.7 ± 12.5 |
| 2000 | 43.4 ± 8.0 |

### Bug 修正記錄

訓練前發現單步 episode 導致 `advantages.std()` 計算 NaN（自由度為 0）的問題。
修正：`advantages.std()` → `advantages.std(correction=0)`（population std），消除 NaN 傳播。

### 結論

- A2C 在單環境下訓練**不穩定**：峰值達 357.5，但隨後策略崩潰退回 ~40
- 根本原因：n_steps=5 的 mini-rollout 更新雜訊大；單一環境無法像原始論文的多環境平行化降低梯度方差
- 對比 PPO（20K steps 達 500 滿分），A2C 需要更多並行 worker 才能穩定
- 演算法完整性：GAE + 優勢歸一化 + entropy bonus + gradient clipping 全部正確實作

---

## CartPole-v1（2026-05-24，VecEnv 重跑）

**動機**：第一次訓練（單環境）僅達 357.5 且不穩定，
改用 4 個平行環境（SyncVectorEnv）以降低梯度估計的方差，
重現 A2C 論文中多工作者平行化的效果。

**程式修改**：
- `agent.py`：新增 `select_actions(states_batch)` 支援批次動作選擇；
  `update()` 改為接受 (T, N) 形狀的陣列，展平後計算 GAE 再更新
- `train.py`：改用 `gym.vector.SyncVectorEnv`（4 envs），
  n_episodes → n_updates（total_steps // (n_envs × n_steps)）

| 引數 | 數值 |
|------|------|
| n_envs | **4**（新增）|
| total_steps | 200,000 |
| lr | 7e-4（RMSprop）|
| n_steps | 5 |
| gae_lambda | 0.95 |
| c_v | 0.5 |
| c_e | 0.01 |
| 有效批次大小 | n_envs × n_steps = **20** |
| 總更新次數 | 10,000 |

**訓練時長**：2026-05-24（< 5 分鐘，CPU）

### 評估回報（每 500 次更新 ≈ 每 10k 步）

| 步數 | 評估回報 |
|---|---|
| 10,000 | 19.3 ± 2.1 |
| 30,000 | 83.6 ± 5.5 |
| 50,000 | 21.3 ± 1.7 |
| 90,000 | 117.8 ± 6.7 |
| 100,000 | 158.3 ± 18.9 |
| 110,000 | **260.7 ± 66.1（峰值）** |
| 120,000 | 32.4 ± 7.5（崩潰）|
| 160,000 | 170.9 ± 6.3 |
| 200,000 | 14.8 ± 6.1 |

### 對比

| 版本 | 峰值 eval | 穩定性 |
|---|---|---|
| 單環境（2026-05-17）| **357.5**（ep 1800）| 崩潰後退回 ~40 |
| VecEnv 4 envs（2026-05-24）| 260.7（step 110k）| 崩潰後退回 ~14 |

### 觀察

```
VecEnv 並未改善 A2C 的根本不穩定性：

問題根源：A2C 沒有「信賴域」保護
  每次更新可能踏出過大的策略步伐
  → 策略破壞後，後續資料品質也下降
  → 強化了崩潰（與 MBPO 的 Q 爆炸類似）

VecEnv 的實際效果：
  ✅ 提供更多樣的梯度（4 個不相關的環境序列）
  ✅ 收斂速度更快（90k 步達 117，單環境需 ~180k 回合才接近）
  ❌ 批次大小增大 4x → 等效學習率提高 → 更新步伐更大 → 崩潰更劇烈

真正的解法：PPO（已完成，CartPole 500 滿分）
  PPO 的 clip(ratio, 1-ε, 1+ε) 限制單步策略更新幅度
  → 即使用 VecEnv，更新也不會踏出致命的一步
```

### Checkpoint

| 步數 | 路徑 |
|---|---|
| 50,000 | `checkpoints/a2c_vecenv_step50000/` |
| 100,000 | `checkpoints/a2c_vecenv_step100000/` |
| 150,000 | `checkpoints/a2c_vecenv_step150000/` |
| 200,000 | `checkpoints/a2c_vecenv_step200000/` |
